spring.application.name=cat

# my sql
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.url=${DB_URL_CAT_LIVE}
spring.datasource.username=${DB_USER_CAT_LIVE}
spring.datasource.password=${DB_PW_CAT_LIVE}

spring.jpa.defer-datasource-initialization=true
spring.jpa.properties.hibernate.format_sql=true

# ==== App Custom ====
app.visitor.cookie-name=vdfm6lkbvndfslkbn3sdf

# ==== Server ====
server.forward-headers-strategy=native

# common auth
allowed_url_csv_list=${SERVER_ALLOWED_URL_CAT_CSV}
